const Migration = require("@aero/aero-record/dist/js/Migration.js").default

class <%= name %> extends Migration {
    async up() {
        // Migration code
    }

    async down() {
        // Migration code
    }
}

module.exports = <%= name %>
